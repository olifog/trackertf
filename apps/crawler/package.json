{
  "name": "@trackertf/crawler",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "seed": "bun run src/seed.ts",
    "sync-schema": "bun run src/sync-schema.ts",
    "typecheck": "tsgo --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@trackertf/db": "workspace:*",
    "@trackertf/steam": "workspace:*",
    "@trackertf/tf2-schema": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0"
  }
}
