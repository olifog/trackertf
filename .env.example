# Local dev (docker-compose.dev.yml postgres):
DATABASE_URL=postgres://trackertf:trackertf@localhost:5432/trackertf

# Steam Web API key (registered to tracker.tf) — never commit the real one.
STEAM_API_KEY=

# VPS deploy only (docker-compose.yml):
POSTGRES_PASSWORD=
